apiVersion: v1
kind: Service

metadata:
  name: faceattendance-service

spec:
  type: NodePort

  selector:
    app: faceattendance

  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
      nodePort: 30007